[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-cleanup"
version = "0.1.0"

[tool.setuptools]
packages = ["customer_data_cleaner"]

[tool.pytest.ini_options]
pythonpath = ["."]